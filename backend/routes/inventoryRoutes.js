import express  from "express";
// import { createInventory, getInventory, createCustomPizzaOrder } from "../controllers/inventoryController";
const router = express.Router();

//route to create a inventory
// router.post("/create/inventory", createInventory);

// router.post("/custom-pizza", createCustomPizzaOrder)

// //route to get the current inventory
// router.get("/get/inventory", getInventory);


// //to update the inventory


export default router;